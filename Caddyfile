{$DOMAIN:localhost}, www.{$DOMAIN:localhost} {
    reverse_proxy /mcp/* neodemos-mcp:8001
    reverse_proxy /* web:8000

    log {
        output file /data/caddy-access.log
        format json
    }
}

neodemos.eu, www.neodemos.eu {
    redir https://neodemos.nl{uri} permanent
}
